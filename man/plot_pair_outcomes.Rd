% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pair_outcomes.R
\name{plot_pair_outcomes}
\alias{plot_pair_outcomes}
\title{Plot match wins and mirrored-pair outcomes}
\usage{
plot_pair_outcomes(
  sage_match_wins,
  gnu_match_wins,
  sage_pair_sweeps,
  tied_pairs,
  gnu_pair_sweeps,
  sage_logo,
  gnu_logo,
  match_length_text = "Seven Point",
  colours = NULL,
  text_colour = NULL,
  muted_text_colour = NULL,
  background_colour = NULL,
  font_family = unname(bms_typography()[["r_fallback"]])
)
}
\arguments{
\item{sage_match_wins}{Number of individual matches won by Sage.}

\item{gnu_match_wins}{Number of individual matches won by GNU.}

\item{sage_pair_sweeps}{Number of mirrored pairs swept by Sage.}

\item{tied_pairs}{Number of mirrored pairs split one match each.}

\item{gnu_pair_sweeps}{Number of mirrored pairs swept by GNU.}

\item{sage_logo}{A grid grob containing the Sage logo.}

\item{gnu_logo}{A grid grob containing the GNU logo.}

\item{match_length_text}{Text used to describe the match length.
Defaults to \code{"Seven Point"}.}

\item{colours}{Named character vector with colours for \code{Sage}, \code{Tied},
and \code{GNU}. \code{NULL} uses the bundled BMS semantic palette.}

\item{text_colour}{Main text colour. \code{NULL} uses the BMS chart token.}

\item{muted_text_colour}{Secondary text colour. \code{NULL} uses the BMS chart
token.}

\item{background_colour}{Plot background colour. \code{NULL} uses the BMS chart
surface token.}

\item{font_family}{Font family used by the figure. Defaults to the portable
R sans-serif fallback. Publication scripts may pass a registered BMS font.}
}
\value{
A \code{ggplot2} plot.
}
\description{
Create the two-bar results figure used for engine benchmark summaries.
The caller supplies already-calculated match and mirrored-pair counts.
Study-specific data loading and interpretation remain outside this
function.
}
\examples{
sage_logo <- grid::circleGrob()
gnu_logo <- grid::circleGrob()

plot_pair_outcomes(
  sage_match_wins = 7,
  gnu_match_wins = 13,
  sage_pair_sweeps = 1,
  tied_pairs = 5,
  gnu_pair_sweeps = 4,
  sage_logo = sage_logo,
  gnu_logo = gnu_logo
)

}
