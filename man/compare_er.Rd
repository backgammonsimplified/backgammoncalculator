% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_er.R
\name{compare_er}
\alias{compare_er}
\title{Compare ER Between Two Engines}
\usage{
compare_er(
  data,
  group_by,
  engine,
  er,
  left_engine = "Sage",
  right_engine = "GNU"
)
}
\arguments{
\item{data}{A data frame containing grouped ER values.}

\item{group_by}{A character vector naming columns that define each
comparison group. Use \code{character()} for one overall comparison.}

\item{engine}{A single character string naming the engine column.}

\item{er}{A single character string naming the ER column.}

\item{left_engine}{A single non-empty character string naming the engine
shown on the left side. Defaults to \code{"Sage"}.}

\item{right_engine}{A single non-empty character string naming the engine
shown on the right side. Defaults to \code{"GNU"}.}
}
\value{
A data frame containing the grouping columns, one ER column for
each engine, the left-minus-right ER difference, and
\code{lower_er_engine}. With the defaults, the metric columns are \code{sage_er},
\code{gnu_er}, and \code{er_difference_sage_minus_gnu}. \code{lower_er_engine} is
\code{"Tie"} when the ER values are equal and \code{NA_character_} when either
side is absent or missing.
}
\description{
Places ER values for two engines side by side within each requested group,
calculates the left-minus-right ER difference, and identifies the engine
with the lower ER.
}
\examples{
grouped <- data.frame(
  component = c("checker", "checker", "cube", "cube"),
  engine = c("Sage", "GNU", "Sage", "GNU"),
  er = c(0.29, 0.18, 9.65, 0.02)
)

compare_er(
  data = grouped,
  group_by = "component",
  engine = "engine",
  er = "er"
)

}
