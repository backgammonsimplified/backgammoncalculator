% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leave_one_out_er.R
\name{leave_one_out_er}
\alias{leave_one_out_er}
\title{Recalculate ER After Leaving Out Each Unit}
\usage{
leave_one_out_er(
  data,
  unit,
  engine,
  equity_lost,
  eligible_decisions,
  left_engine = "Sage",
  right_engine = "GNU"
)
}
\arguments{
\item{data}{A data frame containing unit, engine, and metric columns.}

\item{unit}{A single character string naming the unit to omit in turn, such
as \code{"pair_id"}.}

\item{engine}{A single character string naming the engine column.}

\item{equity_lost}{A single character string naming the normalized-equity-
lost column.}

\item{eligible_decisions}{A single character string naming the eligible-
decision-count column.}

\item{left_engine}{A single non-empty character string naming the left-side
engine. Defaults to \code{"Sage"}.}

\item{right_engine}{A single non-empty character string naming the
right-side engine. Defaults to \code{"GNU"}.}
}
\value{
A data frame with one row per omitted unit. It contains
\code{omitted_value}, \code{remaining_unit_count}, pooled equity loss, eligible
decisions, and ER for both engines, the left-minus-right ER difference,
and \code{lower_er_engine}. Engine-specific column names are derived from the
engine labels.
}
\description{
Performs a descriptive leave-one-unit-out sensitivity calculation. For each
unique unit, the function removes every row belonging to that unit, pools
normalized equity lost and eligible decisions for two engines, recalculates
ER, and compares the remaining ER values.
}
\details{
This function does not produce a bootstrap, confidence interval, or
significance test.
}
\examples{
decisions <- data.frame(
  pair_id = rep(c("pair_01", "pair_02"), each = 2),
  engine = rep(c("Sage", "GNU"), 2),
  equity_lost = c(1, 0.5, 2, 1),
  decisions = c(100, 100, 100, 100)
)

leave_one_out_er(
  data = decisions,
  unit = "pair_id",
  engine = "engine",
  equity_lost = "equity_lost",
  eligible_decisions = "decisions"
)

}
