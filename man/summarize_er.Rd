% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_er.R
\name{summarize_er}
\alias{summarize_er}
\title{Summarize ER}
\usage{
summarize_er(data, group_by, equity_lost, eligible_decisions)
}
\arguments{
\item{data}{A data frame containing the grouping and metric columns.}

\item{group_by}{A character vector naming columns that define the output
groups. Use \code{character()} to calculate one summary across all rows.}

\item{equity_lost}{A single character string naming the normalized-equity-
lost column.}

\item{eligible_decisions}{A single character string naming the eligible-
decision-count column.}
}
\value{
A data frame containing the grouping columns followed by
\code{equity_lost}, \code{eligible_decisions}, and \code{er}. Groups appear in their
first-observed order. A group returns \code{NA_real_} for a pooled metric when
any contributing value for that metric is missing. ER is also
\code{NA_real_} when the pooled eligible-decision count is zero.
}
\description{
Pools normalized equity lost and eligible decisions within groups, then
calculates ER from the pooled numerator and denominator.
}
\details{
This function does not average row-level ER values. For every requested
group it calculates:

\deqn{ER = 500 \times
\frac{\sum equity\ lost}{\sum eligible\ decisions}}
}
\examples{
decisions <- data.frame(
  engine = c("Sage", "Sage", "GNU", "GNU"),
  loss = c(0.4, 0.6, 0.2, 0.3),
  n = c(50, 50, 50, 50)
)

summarize_er(
  data = decisions,
  group_by = "engine",
  equity_lost = "loss",
  eligible_decisions = "n"
)

}
